
let idade = 10;
let idade2 = "20";
let semvalor;

// let valorternario = idade == 20 ? 'idade é igual a 20' : "Idade não é igual a 20";
// console.log(valorternario);


console.log(idade);

    // if (idade !== idade2){
    //     console.log("Entrou no If");
    // } else {
    //     console.log("Não Entrou no If");
    // }

    /*
        Operadores lógicos:
        == -> Igualdade
        === -> Igualdade restrita
        != -> Diferente de
        < -> menor que
        > -> maior que
        <= -> menor ou igual
        >= -> maior ou igual
        && -> AND ou E
        || -> OR ou OU
        ! -> NOT
        ?: -> Ternario
        ?? -> Coelacência Nula
        += -> Soma Atribuída
        -= -> Subtração Atribuída
        ++ => Incremento
        -- -> Decremento
        ... -> Spread

    */

